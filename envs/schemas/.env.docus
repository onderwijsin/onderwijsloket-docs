# @defaultRequired=infer
# @defaultSensitive=false
# ---

# Vercel AI Gateway API key. This is not actually used by the application code, but Docus
# requires it to enabled AI module setup. 
# 
# We are overriding the default AI server handlers where AI Gateway is invoked
# @sensitive
# @required
# @type=string
# @example="my-api-key"
AI_GATEWAY_API_KEY=protonPass(concat(pass://${PROTON_VAULT}/, $APP_ENV, /AI_GATEWAY_API_KEY), allowMissing=true)

